<UserControl x:Class="SenderTest.HeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <CheckBox Name="CbbChecked" Checked="CbbChecked_Checked" VerticalAlignment="Center" />
        <TextBox Name="TxtHeaderKey" Margin="5,0" VerticalAlignment="Center" Grid.Column="1" />
        <TextBox Name="TxtHeaderValue" Margin="5,0" VerticalAlignment="Center" Grid.Column="2" />
    </Grid>

</UserControl>